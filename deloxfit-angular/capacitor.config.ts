import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deloxfit.app',
  appName: 'DeloxFit',
  webDir: 'dist/deloxfit-angular/browser'
};

export default config;
